# Handoff 2026-07-31 14:31 — Crawl komplett durch; Sonderauftrag Factor-Eleven-CD-Auszüge

> Session-Fortsetzung nach Handoff 30.07. 09:25: Sonderauftrag (chronologische
> HR-Auszüge Factor Eleven) erledigt, Crawl-Abschluss der Zielliste bestätigt.

## Stand

**Kein neuer Commit im Repo** (Sonderauftrag lief außerhalb; letzter Stand `0ec2af5`).

**Meilenstein Crawl (31.07.):** Die komplette Zielliste ist durchdossiert.
- GF≥58-Vorrat leer, Daemon läuft im Tail — extrahiert aber fast nichts mehr
  (30.07.: 5.130 brutto, 31.07. bis 14:30: 2). Bestand ~49k Dossiers/Scores.
- Ab jetzt nur noch Nachschub-Arbeit: neue GF≥58-Kandidaten aus laufenden
  hr-engine-Importen (~28k PDFs pending) werden priorisiert dossiert.
- no_text_content-Fix vom 25.07. (`62db1cc`) hat sich bewährt: sauberer
  IDS↔Tail-Wechsel, keine Leerzyklen-Verstopfung mehr.

**Sonderauftrag Factor Eleven (30.07., erledigt):** Chronologische HR-Auszüge (CD)
für Factor Eleven GmbH (AG Gießen HRB 11286) + Factor Eleven Holding GmbH (HRB 11293).
- Umsetzung: Standalone-Punktabruf `/tmp/factor_eleven_cd_abruf.py` —
  Subklasse `DocArtFetcher(HRFetcher)` klickt CD- statt AD-Link (`_result_rows`
  mit parametrisiertem Link-Label); hr-engine-Queue/Daemon unberührt.
- Ablage: `~/projects/os/00-inbox/factor-eleven-hr/` + Kopien in `~/Downloads/`.
- Inhalt: Holding-Struktur 2022/23 via Münchener Vorratsgesellschaft (A. XII
  Beteiligungs GmbH), beide nach Gießen verlegt; Gewinnabführungsvertrag 02.12.2025
  (Holding herrschend); Marc Hundacker 2025 aus beiden GF ausgeschieden
  (Holding 31.07., GmbH 29.12.); aktuelle GF beider: Anhuth + Kühn.

## Offene Punkte

- **JETZT fällig (Crawl durch):** `c6 --rematch` + `--reprio` über den vollen
  Bestand; danach Scoring-Verteilung (A/B/C/KO) für nächste Brief-Welle sichten.
- Wochenlauf `c6 --since 7` überfällig (letzter Fetch 23.07. = 8 Tage → `--since 14`).
- **Sichtung (User, unverändert):** Haupt-Queue 5 neu (Virtus, FB Fliesen,
  Prosegur-Grenzfall, Mauell → irrelevant/Positions-Nachbesetzung,
  Siedlungsgenossenschaft) + Offen-Tabs 122 Gesuche. Dashboard 8502 läuft (nohup).
- contr_fibu-Feintuning, OFFSET-Migrationen, hr-engine-Crash-Muster,
  Phase B (User-Go) — alles unverändert offen, Details im Handoff 30.07.

## Entscheidungen

- **CD-Abruf als Standalone-Subklasse statt Engine-Erweiterung:** fetcher.py klickt
  fest den AD-Link; `abrufart` in Queue/Config ist derzeit toter Parameter. Für
  2 Abrufe lohnt kein Engine-Umbau; Skript liegt als Muster in /tmp (flüchtig —
  bei Wiederbedarf aus diesem Handoff rekonstruierbar bzw. nach scripts/ heben).
- **30 s Abstand zwischen Punktabrufen**, weil der Daemon parallel mit spacing 70 s
  auf demselben Portal fetcht.

## Nächste Schritte

1. `c6 --rematch` + `--reprio` (voller Dossier-Bestand jetzt verfügbar).
2. Wochenlauf `c6 --since 14`.
3. Scoring-Verteilung auswerten → Zuschnitt nächste Brief-Welle (c5).
4. User-Sichtung (Dashboard 8502).
5. Später Phase B (User-Go): Hermes-Cron + Brief »zweite Führungsebene«.

## Kontext

**Pfade:**
- Factor-Eleven-PDFs: `~/projects/os/00-inbox/factor-eleven-hr/`, Kopien `~/Downloads/`
- Punktabruf-Muster: `/tmp/factor_eleven_cd_abruf.py` (überlebt Reboot nicht)
- hr-engine-Fetcher: `~/projects/Unternehmensregister/hr-engine/hr_engine/fetcher.py`
  (`_result_rows` pickt Link per Label; `fetch_ad` benennt Dateien fest `+AD-`)
- Sonst wie Handoff 30.07. (Daemon-Logs, Dashboard, Flag-Stellen).

**Merksatz:** hr-engine-Punktabrufe für beliebige Dokumentarten (CD/HD/…) gehen
ohne Queue-Eingriff über `DocArtFetcher`-Muster; Job ad-hoc aus `models.Job` bauen,
`court_input` wie in der Queue üblich (z.B. "Gießen").

**Offene Fragen:**
- Factor Eleven: Anschlussauftrag? (Kontext des Sonderauftrags unbekannt —
  ggf. Bewerbungs-/Mandatsprüfung, s. untracked `bewerbungen/` im Repo)
- Prosegur SIS vs. Cash Services; Anz.-Spalte >5 Orte; Siedlungsgenossenschaft
  GF-Alter per HR-Abruf? (alle aus Vorsessions)
