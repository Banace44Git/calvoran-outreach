# Handoff 2026-07-21 15:50 — Crawler-Stau aufgelöst: Keyset-Pagination (c3/c4) + Tail-Deckel

> Session-Fokus: Agenten-Check ergab, dass der Crawler-Daemon seit 15.07. in EINEM
> Zyklus steckte (33k-Tail-c3, ~9 Tage Laufzeit) — Ursache Supabase-Timeout 57014 in
> der c3-Selektion. Gefixt, Stau aufgelöst, Rückstau importiert, c4 gleich mitmigriert.

## Stand

**2 Commits (beide gepusht), Ausgangspunkt `86d554b`:**
- `e104e27` — fix(c3): Keyset-Pagination + 57014-Retry in der Selektion, Tail-Deckel je Zyklus
- `6a63d2e` — fix(c4): Loader auf Keyset-Pagination (fetch_all) migriert

**Diagnose (Ausgangslage):**
- hr-engine: lief planmäßig (Worker w1 am 20.07. gecrasht → einkernig, heilt beim Neustart).
- Crawler-Daemon: Prozess lebte, steckte aber seit 15.07. 17:50 im ungefilterten
  Tail-c3 (33.841 Firmen, ~150 Dossiers/h ≈ 9 Tage). Auslöser: `c3 --min-score 2`
  starb an statement timeout (57014, deep OFFSET auf companies) → run_ingest fiel in
  den ungefilterten Lauf. Folge: 5 Tage kein phase_data (kein c1b-Import, keine
  GF≥58-Priorisierung, kein neues c2).

**Fixes:**
- `calvoran/db.py`: neuer Helper `fetch_all(make_query, key, step, retries)` —
  Keyset-Pagination (`key > letzter Wert`, konstante Kosten je Seite) + Backoff-Retry
  bei 57014. Voraussetzung: `key` (Default `id`) in der Select-Liste.
- `pipeline/c3_extract.py`: `select_companies` + `existing_dossier_ids` auf `fetch_all`.
  Verifiziert: volle companies-Selektion 40–75 s statt Timeout.
- `pipeline/run_ingest.sh`: Tail-c3 gedeckelt auf `C3_TAIL_LIMIT` (Default 500) Dossiers
  je Zyklus → Zyklus ~3,5 h statt Tage, phase_data kommt regelmäßig dran.
- `pipeline/c4_score_cluster.py`: `_paginate_ids` + `load_dossiers`-Voll-Load auf
  `fetch_all` (step=500 wegen jsonb). Verifiziert: 32.666 Score-IDs 3,9 s, 32.665 Dossiers 19,2 s.
- os-Doku: `~/projects/os/99-meta/calvoran-crawler.md` Env-Tabelle um C3_TAIL_LIMIT ergänzt.

**Stau-Auflösung (20.07.):** c3-Langläufer per SIGTERM gestoppt → c4 rc=0 (31 min) →
phase_data: consolidate 43 min, c1b ~35.000 Geburtsdaten-Writes (49 min) → GF≥58-Vorrat
neu materialisiert (200 Firmen) → priorisierter IDS-Lauf läuft wieder. Danach
`c6 --rematch` (0 neu, 28 Mehrfach-Dubletten unterdrückt) + `--reprio` (1/312 hochgestuft).

**DB-Stand:** 32.665+ Dossiers (wächst ~1.000/Nacht), nur noch 22 Kandidaten Score ≥ 2
ohne Dossier; job_matches unverändert 312 (3 neu, 293 irrelevant, 15 outreach, 1 abgelehnt).

## Offene Punkte

- **Sichtung (User, unverändert offen):** Haupt-Queue 3 neu (Virtus Group, FB Fliesen
  & Bäder, **Prosegur-Grenzfall 88,9 manuell prüfen**) + Offen-Tabs 122 Gesuche.
- Dashboard lief auf 8502, wurde aber aus dieser Session heraus gestartet — Prozess
  stirbt evtl. mit Session-Ende, ggf. neu starten.
- OFFSET-Pattern existiert noch in c2/c6/Dashboard + Hilfsskripten (c0c, c1, c1b,
  phase1_report, gemma_blind_dossier, materialize_gf58_ids) — Tabellen klein bzw.
  Läufe selten, kein akuter Bedarf; bei 57014-Sichtungen auf `fetch_all` migrieren.
- contr_fibu-Feintuning weiter offen (falls »Leiter Rechnungswesen« beim Sichten rauscht).

## Entscheidungen

- **Keyset statt OFFSET + Retry statt nur Retry:** deep OFFSET wird je Seite teurer,
  Timeout wäre mit wachsendem Bestand wiedergekommen; Retry fängt nur Transientes.
- **Tail-Deckel in run_ingest (C3_TAIL_LIMIT=500) statt Daemon-Umbau:** minimaler
  Eingriff, Daemon-Skript (calvoran-crawler.sh) lief weiter — Editieren am laufenden
  bash-Skript wäre riskant gewesen.
- **c3-Langläufer töten statt auslaufen lassen:** upsert-idempotent/resumebar, und die
  33k-Selektion enthielt überwiegend unpriorisierten Tail, während frische GF≥58-
  Kandidaten 5 Tage liegen blieben.
- **fetch_all zentral in calvoran/db.py:** ein Helper für alle künftigen Migrationen
  (Namenskollision mit modul-lokalen fetch_all-Helfern in c1/c1b/c6 etc. beachten —
  die bleiben vorerst unangetastet).

## Nächste Schritte

1. **User sichtet** (Dashboard 8502): Haupt-Queue 3 + Offen-Tabs 122 Gesuche.
2. Nach Sichtung ggf. contr_fibu-Keywords feintunen + `--rematch`.
3. Wochenlauf `c6 --since 7` fällig (letzter Fetch 15.07. — seither nur rematch/reprio).
4. Nach jedem os-Anreicherungs-Schub: `--rematch` + `--reprio`.
5. Später Phase B (User-Go): Hermes-Cron täglich + Brief »zweite Führungsebene«.

## Kontext

**Pfade:**
- Pagination-Helper: `calvoran/db.py` (`fetch_all`)
- Daemon: `~/projects/os/bin/calvoran-crawler.sh` (launchd `com.jbreuers.calvoran-crawler`),
  Logs `~/.local/state/calvoran-crawler/launchd.out.log` +
  `~/projects/os/01-projects/fractional-cfo/outreach/ingest-<datum>.log` (Datum = Zyklus-START)
- hr-engine: launchd `com.jbreuers.hr-engine`, Logs `~/.local/state/hr-engine/`
- Dashboard: `.venv/bin/streamlit run dashboard/kuratierung.py --server.port 8502`

**Merksatz Agenten-Check:** Prozess lebt ≠ planmäßig — Log-mtimes und Zyklus-Zeilen
im Daemon-Log prüfen (Memory `hintergrund-agenten-betrieb` angelegt).

**Offene Fragen:**
- Prosegur SIS vs. Prosegur Cash Services: dieselbe Zielfirma? (Sichtung)
- Reicht die Anz.-Spalte als Spam-Signal oder ab >5 Orten markieren? (aus Vorsession)
