# Handoff 2026-07-13 20:36 — Job-Signale: irr.-1-Klick, Schema-Doku, Keyword-Gruppen

> Session-Fokus: Dashboard-UX (irr.-Checkbox), neue Schema-Doku-Schicht, und Ausbau des
> Job-Signal-Moduls um eine zweite Keyword-Gruppe (Controlling/Finanz-Leitung) mit eigenem Tab.

## Stand

**4 Commits (alle gepusht), Ausgangspunkt `0945e27`:**
- `85aae82` — 1-Klick-»irr.«-Checkbox in Job-Signale- + TEMP-Tab
- `0ffc557` — OKF-schlanke Schema-Doku `docs/schema/` + Drift-Check
- `810f273` — Fix: irr.-Häkchen sammeln statt pro Klick schreiben (3-5s-Lag weg)
- `0abae9a` — Keyword-Gruppen kfm.Ltg. / Contr./Fibu + eigener Tab

**Geänderte/neue Dateien:**
- `dashboard/kuratierung.py` — irr.-CheckboxColumn (Batch über Speichern-Button); TEMP-Tab →
  parametrisierte Funktion `render_jobsignal_offen(gehoert, key_prefix)` + `load_keyword_gruppen()`;
  6 Tabs (»Job-Signale kfm.Ltg.«, »Job-Signale Contr./Fibu«).
- `config/jobsignale.yaml` — Umbau flache `keywords` → `keyword_gruppen` (kfm_ltg + contr_fibu),
  je Gruppe eigene `titel_positiv`; gemeinsame Negativlisten + junior/werkstudent/praktik/studentische.
- `pipeline/c6_jobsignale.py` — `titel_regeln` gibt jetzt `gruppen_positiv`-dict; `api_pull` scannt
  je Gruppe mit deren Positivliste; `cmd_rematch` prüft gegen Vereinigung aller Positivlisten.
- `docs/schema/` (NEU) — `index.md` + 10 Tabellen-MDs + `check_drift.py`.
- `CLAUDE.md` — Verweis auf docs/schema/, Migrations-Range 0001..0009, Tab-Liste + Keyword-Gruppen.

**DB-Stand jetzt (Schema calvoran):** 421 job_postings, 328 job_matches
(27 neu, 286 irrelevant, 14 outreach, 1 abgelehnt).

## Offene Punkte

- **Sichtung wartet (NICHT UMGESETZT, User-Aufgabe):** Dashboard auf 8502 neu laden →
  **Contr./Fibu-Tab: 109 offene Anzeigen** (64 Leiter Rechnungswesen + Leiter Finanzen/Controlling
  + inhaltlich passende Fachkraft-keyword-Reste); **Haupt-»Job-Signale«-Queue: 27 neu**.
  kfm.Ltg.-Tab ist 0 offen (durchgesichtet).
- **~45 verwaiste Alt-Postings** in der DB mit Fachkraft-keywords (Controller/Finanzbuchhalter/
  Bilanzbuchhalter) aus dem verworfenen breiten Backfill. Erscheinen korrekt im Contr./Fibu-Tab
  (Prädikat »nicht-kfm«), da ihr Titel eine Leitungs-Regel besteht. Kein Handlungsbedarf.
- **contr_fibu-Feintuning möglich:** falls »Leiter Rechnungswesen« zu viel Rauschen bringt oder
  »Controller« doch gewünscht — Zweizeiler in `config/jobsignale.yaml` + `--rematch`.

## Entscheidungen

- **irr. Batch statt Sofort-Write** (`810f273`): der on_change-Callback rief bei jedem Klick
  `load_job_signale.clear()` + Full-Page-Rerun → 3-5s/Klick. Jetzt sammeln + einmal speichern.
- **Schema-Doku = OKF-Kern, nicht voller Standard:** eine MD/Tabelle, statisch aus Migrationen,
  + `check_drift.py` (PostgREST-OpenAPI vs. dokumentierte Spalten; kein PG-Direktzugang nötig).
- **Keyword-Gruppen mit je eigener Titel-Positivliste:** die globale Liste hätte alle Controlling/
  Finanz-Anzeigen verworfen. Kategorie aus `keyword` abgeleitet → **kein DB-Schema-Change**.
- **contr_fibu NUR Leitungsebene** (Leiter Rechnungswesen/Finanzen/Controlling): Controller/Finanz-/
  Bilanzbuchhalter als Einzelfachkräfte fluteten (>3800 offene Anzeigen, >4000 gesamt) und sind
  als Übergabe-Signal zu schwach. User-Entscheid gegen das breite Set.
- **Haupt-Queue gemischt lassen** (User-Entscheid): bei einer Zielfirma ist jedes Führungs-/Finanz-
  Gesuch relevant, Kategorie egal → kein Kategorie-Filter im Haupttab.
- **Contr./Fibu-Tab-Zuordnung = »nicht-kfm«** (statt »in cf-keywords«): fängt auch verwaiste
  Alt-Fachkraft-keywords, die inhaltlich Leitung sind; zukunftssicher gegen eine 3. Gruppe via
  `kw in _CF_KW or kw not in _ALLE_KW`.

## Nächste Schritte

1. **User sichtet:** Contr./Fibu-Tab (109) + Haupt-Queue (27 neu) — Batch-irr. nutzen.
2. Nach Sichtung ggf. contr_fibu-Keywords/Positivliste feintunen + `--rematch`.
3. Nach jedem os-Anreicherungs-Schub: `c6_jobsignale.py --rematch` + `--reprio`.
4. Nach neuer SQL-Migration: betroffene `docs/schema/*.md` mitziehen + `check_drift.py`.
5. Später Phase B (User-Go): Hermes-Cron täglich + Brief »zweite Führungsebene« über c5-Mechanik.

## Kontext

**Pfade:**
- Modul: `pipeline/c6_jobsignale.py`, `calvoran/ba_jobsuche.py`, `calvoran/matching.py`, `config/jobsignale.yaml`
- Dashboard: `dashboard/kuratierung.py` (`render_jobsignal_offen`, `load_keyword_gruppen`;
  Start `.venv/bin/streamlit run dashboard/kuratierung.py --server.port 8502`)
- Schema-Doku: `docs/schema/` (`index.md`, 10 Tabellen-MDs, `check_drift.py`)

**CLI-Betrieb c6:**
```
.venv/bin/python pipeline/c6_jobsignale.py --backfill   # Voll-Sweep, 7 Keywords in 2 Gruppen
.venv/bin/python pipeline/c6_jobsignale.py --since 7    # Wochenlauf, idempotent
.venv/bin/python pipeline/c6_jobsignale.py --rematch    # Filter-/Keyword-Tuning ohne API
.venv/bin/python pipeline/c6_jobsignale.py --reprio     # Prio aus aktuellem gf_alter
.venv/bin/python docs/schema/check_drift.py             # Schema-Doku vs. Live (Exit 1 bei Drift)
```

**Offene Fragen:**
- Bringt »Leiter Rechnungswesen« (64 der 109) beim Sichten zu viel Rauschen? → ggf. enger fassen.
- Mehrstädte-Spam-Auto-Erkennung (gleicher Arbeitgeber+Titel >5 Orte) weiterhin offen.
