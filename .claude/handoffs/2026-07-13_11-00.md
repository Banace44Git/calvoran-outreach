# Handoff 2026-07-13 11:00 — Job-Signal-Modul (BA-Stellenanzeigen) Phase A

> Diese Session: komplettes neues **Job-Signal-Modul c6** gebaut (BA-Jobsuche →
> Nachfolge-Signal) inkl. Dashboard-Tabs, plus laufende Sichtungs-/Pflege-Ops.
> Parallel liefen fremde Commits (os-Anreicherung / Dauer-Crawler) in denselben Branch.

## Stand

**Job-Signal-Modul c6 fertig und produktiv** (Commits `bf7e444` … `8ad45ba`, alle gepusht).
Idee: Sucht eine Zielfirma per Stellenanzeige GF / kaufm. Leitung / CFO, ist das bei
Inhabern 58+ ein Übergabe-Indikator. BA-Jobsuche-API (kostenlos), Match gegen `companies`.

**Neue/­geänderte Dateien:**
- `calvoran/ba_jobsuche.py` (NEU) — v6-API-Client. **Eigenheiten:** Antwortformat weicht von
  Community-Doku ab (`ergebnisliste`/`referenznummer`/`firma`); `veroeffentlichtseit` kennt
  NUR 1/7/14/28 (andere Werte → still ungefiltert → Client snappt); `--backfill` lässt den
  Parameter weg = Voll-Sweep aller aktiven Anzeigen (Langläufer!); lautes Scheitern bei 401/403.
- `calvoran/matching.py` (NEU) — `norm_firma()` (Rechtsform-Phrasen), PLZ-Blocking, Stufen
  exakt/fuzzy/fuzzy_grenzfall/region + `extern`. Token-Teilmengen nie Auto-Match (Konzern-Spam).
- `pipeline/c6_jobsignale.py` (NEU) — CLI `--backfill/--since/--dry-run/--rematch/--reprio/--report`.
- `config/jobsignale.yaml` (NEU) — Keywords, Titel-Positiv/Negativ + BA-Beruf-Negativ,
  `exclude_arbeitgeber` (EY Consulting, ENERDOMO).
- `dashboard/kuratierung.py` (GEÄNDERT) — 2 neue Tabs: **»Job-Signale«** (Review-Queue,
  Status/Notiz-Write-Back) und **»Job-Signale TEMP«** (Anzeigen OHNE Zielliste-Match →
  editierbar, Status setzen erzeugt externen Signal-Lead). Plus »Alle Filter entfernen«-Button
  im Haupt-Tab, Mehrwort-Suche (Name/Ort/PLZ).
- `sql/migrations/0007..0009` (NEU) — job_postings/job_matches; company_id nullable +
  Stufe `extern` (0008); Status `abgelehnt` (0009). **Alle 3 vom User in Supabase angewandt.**
- `CLAUDE.md`, `requirements.txt` (rapidfuzz) aktualisiert.

**DB-Stand jetzt:** 281 Anzeigen, 96 Matches — 8 outreach, 1 abgelehnt, 87 irrelevant;
74 externe (company_id NULL), 14 fuzzy_grenzfall, 8 exakt. Der User hat aktiv gesichtet
(87× irrelevant → Motivation für die offene Aufgabe unten).

**Manuelle Ops diese Session:** IGK Meschede / Bäckerei Fahland / Bezirksverein für soziale
Rechtspflege → `outreach`; Baldus Medical → `abgelehnt` (Absage). EY-Consulting-Anzeige +
19 ENERDOMO-Anzeigen gelöscht (Franchise-Spam). Backfill/rematch/reprio gelaufen.

## Offene Punkte

- **NICHT UMGESETZT (letzte User-Aufgabe, hat Vorrang):** Im Tab »Job-Signale« eine neue
  Spalte **»irr.«** rechts neben `Status` — nur eine **Checkbox**, ein Klick = Wirkung von
  Status `irrelevant`. User will Ein-Klick statt Selectbox+Speichern. Details in Memory
  `offene-aufgabe-irr-checkbox`.
- Fremde Commits `8e2d837` (HTTP/2-Reconnect) + `1b8c761` (Dauer-Crawler) sind NICHT aus
  dieser Session — parallele os-/Crawler-Arbeit im selben Branch. Nicht anfassen ohne Klärung.
- `st.components.v1.html` deprecated (pre-existing, Warnung zur Laufzeit).

## Entscheidungen

- **Eigene Tabellen job_postings/job_matches** statt `signals` (das wird von c3 je Firma per
  delete+insert überschrieben) oder `raw_jobs` (tote Apify-Semantik).
- **Externe Signal-Leads = job_matches mit company_id NULL, Stufe `extern`** (0008): Firmen aus
  dem TEMP-Tab (nicht in Zielliste) wandern per Status in die Arbeitsliste, ohne companies-Eintrag
  (die Zielliste gehört dem os-Import).
- **Status `abgelehnt`** (0009) als eigener Endzustand ≠ irrelevant (Match-Müll) ≠ outreach (aktiv).
- **Titel-Negativliste gegen Stellentitel, Beruf-Negativliste gegen BA-Hauptberuf** getrennt —
  BA klassifiziert echte kaufm. Leitungen als »Betriebsleiter/in - kaufmännisch«.
- **`order('id')` in ALLEN paginierten Voll-Abrufen** (Fix `8ac95b3`): ohne ORDER BY verlor die
  Pagination bei parallelen os-Writes still ganze Teilmengen (45k statt 70k companies).

## Nächste Schritte

1. **ZUERST: 1-Klick-»irr.«-Checkbox** im Job-Signale-Tab bauen (`dashboard/kuratierung.py`,
   `data_editor` des Tabs, CheckboxColumn nach Status; Häkchen → status=irrelevant + reviewed_at).
   Idealerweise sofortiges Write-Back ohne Extra-Button. Memory `offene-aufgabe-irr-checkbox`.
2. Ggf. dieselbe Checkbox im TEMP-Tab — erst beim User rückfragen.
3. Nach jedem os-Anreicherungs-Schub: `c6_jobsignale.py --rematch` + `--reprio`
   (Memory `gf-alter-anreicherung-extern`).
4. Später Phase B (nach Sichtungsmonat, User-Go): Hermes-Cron täglich + Brief »zweite
   Führungsebene« über c5-Mechanik, A/B über `outreach.variant`.

## Kontext

**Pfade:**
- Modul: `pipeline/c6_jobsignale.py`, `calvoran/ba_jobsuche.py`, `calvoran/matching.py`,
  `config/jobsignale.yaml`
- Dashboard: `dashboard/kuratierung.py` (Start: `.venv/bin/streamlit run dashboard/kuratierung.py
  --server.port 8502`; 8501 = Fremdprojekt)
- Migrationen: `sql/migrations/0007_job_postings.sql`, `0008_job_matches_extern.sql`,
  `0009_job_matches_abgelehnt.sql` (alle angewandt)

**Supabase (Schema `calvoran`):**
- `job_postings` (refnr unique, raw jsonb) / `job_matches` (posting_id, company_id NULLABLE,
  match_stufe inkl. `extern`, status inkl. `abgelehnt`, prio, status_notiz)
- Reguläre Matches: (posting_id, company_id) unique; externe: partieller Unique auf posting_id
  where company_id is null

**CLI-Betrieb:**
```
.venv/bin/python pipeline/c6_jobsignale.py --backfill    # Voll-Sweep aller aktiven Anzeigen
.venv/bin/python pipeline/c6_jobsignale.py --rematch     # neu matchen (ohne API), nur status='neu'
.venv/bin/python pipeline/c6_jobsignale.py --reprio      # Prio aus aktuellem gf_alter
.venv/bin/python pipeline/c6_jobsignale.py --report      # KPI-Markdown nach OUTPUT_DIR
```

**Offene Fragen:**
- Auto-Erkennung von Mehrstädte-Spam (gleicher Arbeitgeber+Titel an >5 Orten → Warn/Drop)?
  Beim User angeboten, noch offen.
- Zielliste um passende TEMP-Nicht-Matches erweitern (Signal-first-Leads)?
