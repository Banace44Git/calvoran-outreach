# Handoff 2026-07-30 09:25 — no_text_content-Flag: Vorrat-Verstopfung gelöst, Crawl fast durch

> Session-Fokus: c6-Wochenlauf (2 neue Matches), Mauell-Lesart-Korrektur, dann
> Hauptarbeit: GF≥58-Vorrat war durch 200 Blindgänger dauerverstopft (reachable,
> aber keine Text-Seiten) — geflaggt, ausgeschlossen, Durchsatz verfünffacht.

## Stand

**1 Commit (gepusht), Ausgangspunkt `960171a`:**
- `62db1cc` — fix(crawl): no_text_content-Flag — Blindgänger verstopfen den GF>=58-Vorrat nicht mehr

**Vorrat-Verstopfung (25.07. diagnostiziert + gefixt):**
- Ursache: c2 setzt `reachable=true`, sobald die Site antwortet — trafilatura
  extrahiert aber bei Frameset/JS-only/Platzhalter nichts → alle `pages` ohne
  `text_content` → nie Dossier (c3-Skip `keine_seiten_mit_text`) → nie Score →
  Firma bleibt ewig im Vorrat. 200 solcher Blindgänger belegten die id-sortierte
  Queue komplett; Tail kam seit 23.07. nicht dran, 596 echte GF≥58-Kandidaten
  verhungerten dahinter. Betroffen gesamt: 1.126 von 48.720 reachable (315 GF≥58).
- Fix (4 Teile): `pipeline/flag_no_text_content.py` (idempotenter Backfill, 1.126
  geflaggt); c2 `persist()` stempelt Flag künftig beim Crawl; `materialize_gf58_ids`
  + c3 `select_companies` schließen Geflaggte aus (c3-Filter schützt auch
  Tail-Limit-Slots vor Leer-Skips). Doku: `docs/schema/companies.md`, os-Meta.
- Wirkung: ~2.400–2.600 neue Dossiers/Tag (vorher ~500). Stand 30.07. 09:25:
  **46.046 Dossiers**, ungecrawlt 0, c3-Rest ~1.300 → **heute komplett durch**.
  Daemon wechselt sauber IDS-Lauf ↔ Tail (WEB_TAIL=1).

**c6-Wochenlauf 23.07. (`--since 14` wegen 8-Tage-Lücke):** 114 Anzeigen, 2 neue
Matches (beide exakt): Mauell GmbH (Velbert, GF 59, prio=hoch) + Siedlungs-
genossenschaft Augsburg-Firnhaberau eG (prio=unbekannt).

**Mauell-Korrektur (User, 25.07.):** Anzeige besetzt die Rewe-Leitung altersbedingt
NACH → Positions-Nachbesetzung, KEIN Inhaber-Übergabe-Signal. Heuristik als Memory
`jobsignal-nachfolge-lesart` gesichert. Match steht noch auf »neu«.

**Agenten:** hr-engine 2 Worker, ~46k done / ~28k pending (Playwright-Crash
„Execution context destroyed" tritt wiederholt auf, Daemon heilt sich selbst —
beobachten). Streamlit 8502 läuft (nohup, überlebt Session-Ende).

## Offene Punkte

- **Sichtung (User, unverändert):** Haupt-Queue jetzt 5 neu (Virtus, FB Fliesen,
  Prosegur-Grenzfall 88,9, Mauell → als irrelevant/Positions-Nachbesetzung
  einstufen, Siedlungsgenossenschaft) + Offen-Tabs 122 Gesuche.
- **Nach Crawl-Abschluss (heute):** `c6 --rematch` + `--reprio` über vollen
  Bestand; Scoring-Verteilung (A/B/C/KO) für nächste Brief-Welle sichten.
- Wochenlauf `c6 --since 7` wieder fällig (letzter Fetch 23.07.).
- contr_fibu-Feintuning offen (falls »Leiter Rechnungswesen« rauscht).
- OFFSET-Pattern weiter in c2/c6/Dashboard + Hilfsskripten (bei 57014 auf
  `fetch_all` migrieren). DB-Counts unter consolidate-Last laufen in 57014 —
  für Statuszahlen Planner-Count nur mit Vorsicht (liefert teils Unsinn, s.u.).
- hr-engine w0/w1-Crash-Muster: falls Frequenz steigt, try/except um das
  betroffene `page.evaluate` (Code liegt in ~/projects/Unternehmensregister/hr-engine).
- Phase B Job-Signale (Hermes-Cron + Brief) wartet auf User-Go.

## Entscheidungen

- **Flag in tech_signals statt neuer Spalte/Migration:** kein Schema-Change nötig;
  Re-Crawl mit Text überschreibt tech_signals komplett → Flag verschwindet von
  selbst (selbstheilend). Nur `true` wird gestempelt, Filter = `is null`.
- **Ausschluss an BEIDEN Selektionsstellen** (materialize + c3): sonst hätte
  dieselbe Bug-Klasse später den Tail deadlocked (Skips fressen --limit-Slots).
- **Kein run_ingest-Fallback (IDS→Tail bei 0 Dossiers):** materialize-Fix reicht,
  Diff minimal halten; Daemon-Wrapper lief unangetastet weiter.
- **`--since 14` statt 7:** 8-Tage-Lücke seit 15.07., API snappt aufwärts, idempotent.
- **Mauell-Heuristik als Memory statt c6-Textklassifikator:** ~2 Matches/Woche,
  Volltext nur via Detail-Endpoint, Phrase zweischneidig — manueller Blick billiger.

## Nächste Schritte

1. Crawl-Abschluss verifizieren (c3-Rest ~1.300, heute), dann `c6 --rematch` + `--reprio`.
2. User sichtet (Dashboard 8502): Haupt-Queue 5 + Offen-Tabs; Mauell einstufen.
3. Wochenlauf `c6 --since 7`.
4. Scoring-Verteilung für nächste Brief-Welle auswerten (c4-Report / Dashboard).
5. Später Phase B (User-Go): Hermes-Cron + Brief »zweite Führungsebene«.

## Kontext

**Pfade:**
- Flag-Backfill: `pipeline/flag_no_text_content.py` (rerunnbar, --dry-run)
- Flag-Stellen: `pipeline/c2_crawl.py` persist(), `pipeline/materialize_gf58_ids.py`
  gf58_backlog(), `pipeline/c3_extract.py` select_companies()
- Daemon: `~/projects/os/bin/calvoran-crawler.sh` (setzt PYTHONPATH — manuelle
  materialize-Läufe brauchen `PYTHONPATH=.`), Logs wie gehabt
  (`~/.local/state/calvoran-crawler/`, ingest-<datum>.log)
- Dashboard: läuft auf 8502 (nohup, /tmp/streamlit-8502.log)

**Merksätze:**
- Planner-Counts (`count="planned"`) auf gefilterten jsonb-Queries liefern teils
  absurde Werte (1 statt 47k) — exakt zählen, wenn DB idle, sonst ableiten.
- BA-API: `veroeffentlichtseit` nur 1/7/14/28, Client snappt aufwärts.

**Offene Fragen:**
- Prosegur SIS vs. Cash Services: dieselbe Zielfirma? (Sichtung)
- Anz.-Spalte als Spam-Signal ausreichend oder ab >5 Orten markieren? (Vorsession)
- Siedlungsgenossenschaft Augsburg: GF-Alter unbekannt — lohnt HR-Abruf?
